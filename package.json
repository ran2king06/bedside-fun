{
  "name": "bedside-finds",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "generate:site": "nuxt generate && node scripts/copy-static.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "4.5.2",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
